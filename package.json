{
  "name": "conduitos",
  "version": "0.1.0",
  "description": "Local TypeScript CLI for routing autonomous agent operations through policy, trace, and memory.",
  "type": "module",
  "private": true,
  "scripts": {
    "conduit": "tsx src/commands/conduit-cli.ts",
    "conduitos": "tsx src/commands/conduit-cli.ts",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "pino": "^9.5.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "tsx": "^4.19.1",
    "typescript": "^5.6.3",
    "vitest": "^4.1.7"
  }
}
